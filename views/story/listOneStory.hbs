{{#if story }}
    {{#with story}}
        <div>
            <h1>{{ title }}</h1>
        </div>
        <div>
            <h2> {{ createdAt }} </h2>
        </div>
        <div>
            <p> {{ content }} </p>
        </div>

        {{!-- {{#if edit}}

        {{/if}} --}}
        {{!-- <form method="GET" action="../../routes/story">
               <input type="submit" value='Update' class='btn-story'>
        </form> --}}
        
        <form class= "" action = "/story/{{id}}?_method=DELETE" method="POST">
        <input type="submit" value="Remove">
        </form>
    {{/with}}
{{/if}}

